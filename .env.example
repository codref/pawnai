# HuggingFace API token for downloading models
# Get yours from: https://huggingface.co/settings/tokens
HF_TOKEN=your_hf_token_here

# Optional: Device configuration (auto, cuda, cpu)
# DEVICE=auto

# Optional: Database path
# DB_PATH=speakers_db

# Optional: Audio directory
# AUDIO_DIR=audio

# Usage:
# 1. Copy this file to .env and fill in your token
# 2. Or specify the config file with --config flag:
#    pawnai diarize audio.wav --config ./custom/.env
#    pawnai transcribe audio.wav --config /path/to/.env

