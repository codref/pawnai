"""Domain tool implementations for sallm CliTools.

Production tools are CLI entrypoints under ``pawn_agent/tools/cli/`` that call
``*_impl`` functions in the sibling modules here. There is no PydanticAI
auto-discovery registry anymore.
"""
